<%- include('_table', { requests, user, statusBadgeClass, totalCount, filteredCount, filters, filterOptions, hasActiveFilters, sort, cardSortOptions, sortToggleUrl, sortToggleListUrl, isSortActive, clearListUrl, pagination, paginationUrl, paginationListUrl }) %>
