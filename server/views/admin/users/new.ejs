<%- include('../../partials/layout', { pageTitle: 'New user', pageIcon: 'plus' }) %>

    <div class="mb-6">
      <h1 class="rh-page-title mb-2">New user</h1>
      <p class="rh-subtitle">Create a team member account with role and optional Telegram ID.</p>
    </div>

    <%- include('_form', { formAction: '/admin/users', isNew: true, formUser, teamleads, error }) %>

<%- include('../../partials/foot') %>
